[project]
name = "api"
version = "0.1.0"
description = "REST API per il layer Gold (MongoDB) e l'Anomaly Detection VitalDB"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.128.1",
    "pymongo>=4.6",
    "pandas>=2.0",
    "scikit-learn>=1.4",
    "python-dotenv>=1.0",
]
